/** Shared discriminated unions used across the simulator (client + server). */
export type OfferTier = "economique" | "hautStanding" | "prestige";
export type BuildType = "plainPied" | "r1" | "r2";
export type TerrainTopography = "flat" | "slightSlope" | "steepSlope";
